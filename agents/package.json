{
  "name": "agent-rover-windows-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Windows native agent for agent-rover",
  "scripts": {
    "build": "make -j",
    "clean": "make clean",
    "pack": "npm run build && ./pack.sh",
    "test": "vitest run"
  },
  "devDependencies": {
    "screw-up": ">=1.34.0",
    "vitest": ">=2.0.0"
  }
}
